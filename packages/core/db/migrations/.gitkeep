# Numbered migrations NNN_name.sql go here (applied lexicographically, one transaction
# each, fail-closed). M1's full schema is the baseline in ../schema.sql, so this
# directory is empty at M1. Do not add BEGIN/COMMIT — the runner owns the transaction.
