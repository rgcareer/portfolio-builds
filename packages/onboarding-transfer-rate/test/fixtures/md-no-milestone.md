# Bolt

## Getting started

Install:

```bash
npm install bolt-runtime
```

Create a file `main.js`:

```js
import { run } from 'bolt-runtime';
run();
```

Run it with `node main.js`.

## Configuration

See `bolt.config.js`.
